import { ChannelSelectPipe } from './channel-select.pipe';

describe('ChannelSelectPipe', () => {
  it('create an instance', () => {
    const pipe = new ChannelSelectPipe();
    expect(pipe).toBeTruthy();
  });
});
